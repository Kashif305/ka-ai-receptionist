import { DashboardLoading } from "@/components/dashboard-loading";

export default function Loading() {
  return (
    <DashboardLoading
      title="Appointments"
      description="Loading salon bookings."
    />
  );
}
