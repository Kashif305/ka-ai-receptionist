import { DashboardLoading } from "@/components/dashboard-loading";

export default function Loading() {
  return <DashboardLoading title="Business Hours" description="Loading schedule settings." />;
}
